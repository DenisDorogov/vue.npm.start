import { createStore } from 'vuex';
import userModule from './modules/user/index';

export default createStore({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    user: userModule
  }
})
